{
  "name": "nguyen-gia-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm --prefix client run build",
    "build": "npm --prefix client run build",
    "start": "node server/server.js",
    "postinstall": "npm --prefix client install"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "workspaces": [ "server", "client" ]
}
